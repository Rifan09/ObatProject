<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hasil Rekomendasi</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 flex justify-center items-center h-screen">
    <div class="container w-11/12 max-w-md bg-white p-6 rounded-lg shadow-md">
        <h1 class="text-2xl font-bold text-center text-gray-800 mb-6">Hasil Rekomendasi</h1>
        <% if (result && Array.isArray(result)) { %>
            <div class="mt-4 p-4 bg-green-100 border border-green-400 text-green-700 rounded">
                <h2 class="font-bold">Rekomendasi Obat:</h2>
                <ul>
                    <% result.forEach(function(item) { %>
                        <li>- <%= item %></li>
                    <% }); %>
                </ul>
            </div>
        <% } else if (error) { %>
            <div class="mt-4 p-4 bg-red-100 border border-red-400 text-red-700 rounded">
                <strong>Error:</strong> <%= error %>
            </div>
        <% } %>
        

        <div class="mt-4">
            <a href="/sirek" class="block text-center bg-blue-900 text-white font-semibold py-2 px-4 rounded-lg hover:bg-blue-800">Coba Lagi</a>
        </div>
    </div>
</body>
</html>
